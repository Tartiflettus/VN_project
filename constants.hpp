#ifndef DEF_CONSTANTS_HPP
#define DEF_CONSTANTS_HPP

const sf::Vector2f WINDOW_SIZE = sf::Vector2f(1280, 720);

//Area in the text box used to display text
const sf::FloatRect TEXTBOX_RECT = sf::FloatRect(40, 528, 1200, 144);


const sf::Vector2f NAME_POS = sf::Vector2f(40, 528-30);


#endif //DEF_CONSTANTS_HPP

